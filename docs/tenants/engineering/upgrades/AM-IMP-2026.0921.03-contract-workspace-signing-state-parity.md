# AM-IMP-2026.0921.03 — Contract workspace signing-state parity

The Engineering contract workspace now merges the authoritative contract control projection before deciding whether to show Party A assignment, internal confirmation, or final artifact controls.

The change is read-only until an authorized operator explicitly presses the existing finalization action. It requires no schema migration, environment change, signer reassignment, re-signing, LINE resend, or evidence rewrite.
