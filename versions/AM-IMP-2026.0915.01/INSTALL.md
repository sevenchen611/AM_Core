# Install

1. Deploy the updated `modules/construction/dashboard.js`.
2. Restart or redeploy the AM Platform service.
3. Do not rerun the design-drawing provisioning script and do not delete or
   recreate the existing Drive folder or Notion data source.

This is a code-only hotfix for `AM_PLATFORM:engineering`.
