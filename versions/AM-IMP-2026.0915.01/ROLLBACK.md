# Rollback

Redeploy the immediately preceding AM Platform commit.

Do not delete Google Drive files or Notion design-drawing index rows. The bug is
limited to the post-success browser flow; stored versions are valid evidence and
remain recoverable after rollback.
