# Install

1. Deploy the updated AM Platform runtime.
2. No database migration or environment change is required.
3. Reload the Engineering contract workspace. It fetches `/contracts/api/v2/contracts/:contractId/control` before rendering signing actions.
