# Rollback

Revert the runtime commit and redeploy the prior AM Platform release. No schema or contract evidence rollback is required because this change only affects authoritative read-model projection and workspace rendering.
