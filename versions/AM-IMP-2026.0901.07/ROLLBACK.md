# Rollback

1. Disable formal signing before rolling the runtime back so an older page cannot accept incomplete contractor data.
2. Preserve all existing contractor details, identity documents, signatures, evidence snapshots, signed PDFs, and receipts.
3. Do not rewrite or delete previously generated immutable evidence.
4. Restore the package only after required-field, Word-table, payment-table, footer, and signed-PDF checks pass again.
