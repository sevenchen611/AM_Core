# Rollback

Restore the previous contract signing service and web modules, then redeploy
the project-local runtime. Verified non-signer group members will return to PDF
only read access and the signer layout will again remain hidden.

Rollback does not require changing the database, signing token, designated
signer, LINE group binding, contract record, or any stored evidence.

