# Verify

1. Open attachment identifiers 0 through 5 from the same contract version; none may fail with `PATH_BODY_REFERENCE_MISMATCH`.
2. Open a protected LINE archive and verify its archive identifier does not collide with the version identifier.
3. Hover an attachment chip and confirm the × control appears; on touch devices it remains visible.
4. Confirming × creates V(n+1), removes that file from the new package, and leaves Vn unchanged.
5. Create another version and verify the excluded file does not reappear from older cumulative history.
6. Confirm the Drive source was not physically deleted.
7. Run the targeted dry-runs and package validator.
