# Rollback

Revert the merged AM Platform commit through a pull request and let Render deploy the reverted `main` commit. Preserve all Finance Claims v3 queue, ingress, and delivery-ledger rows for reconciliation; no database rollback is required.
