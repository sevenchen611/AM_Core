# Install

1. Deploy the updated AM Platform runtime.
2. No database migration or environment variable is required.
3. Existing completed contracts work immediately because their immutable signing-session evidence is used.
4. Run `npm run dryrun:contract-identity-attachments`.
