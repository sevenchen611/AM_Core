# Rollback

Revert the runtime commit that adds the identity-document routes and workspace links, then redeploy.

No database rollback, file deletion or data repair is required. The existing private identity evidence remains untouched and continues to be used by the signed PDF and verification flow.
