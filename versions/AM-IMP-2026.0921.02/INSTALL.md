# Install

1. Deploy the reviewed AM Platform commit from `main`.
2. Confirm production health returns HTTP 200 with the HOZO AM 2.0 claims module loaded.
3. Have the external vendor return to the original group and enter `請款` again so a fresh short-lived selector is created.
4. Confirm the published legacy form opens after LINE identity verification.

No database migration, environment change, Notion binding, Rental deployment or
external Finance V3 membership is required.
