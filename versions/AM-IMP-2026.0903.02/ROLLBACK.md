# Rollback

Remove the read-only routes and UI section if required. The reducer and
control center do not write any contract data, so no authority-data rollback
is necessary. Preserve the production evidence gathered during verification.
