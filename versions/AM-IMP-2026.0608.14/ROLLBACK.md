# Rollback

Disable the checkbox-direct-task rule in the project-local meeting sync script.

Rollback should restore the previous behavior where meeting lines must pass the ordinary action-item judgment before task creation.

Do not delete project-local tasks created from real meeting checkbox items unless the project owner explicitly requests cleanup.
