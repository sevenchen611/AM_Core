# Install

1. Create `docs/project-improvement-manifest.md`.
2. Create `docs/upgrades/`.
3. Add shared registry pointer document.
4. Record project-specific data boundary rules.
5. Update shared registry.

