# Verify

- `node --check src/control-api.js`
- Preview each report type if the control API can run locally:
  - `morning`
  - `followup-morning`
  - `followup-midday`
  - `followup-afternoon`
  - `daily`

Pass when all report messages include goal recognition / goal confirmation language.

