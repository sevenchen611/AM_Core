# AM-IMP-2026.0831.02 — Engineering contract draft and review controls

Engineering contract managers can return an `internal_review` version to `draft`, correct it, and generate a new draft-review message for the bound LINE group without creating a replacement version.

The workspace now confirms successful transitions, identifies the failed draft-review stage in Chinese, logs safe workflow error metadata, and shows the real contract-version workflow state in the overview before a signing session exists.
