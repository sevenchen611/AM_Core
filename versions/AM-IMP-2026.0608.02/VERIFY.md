# Verify

- `node --check src/control-api.js`
- Preview report without sending.
- Confirm resolved target count can be greater than one.
- Confirm targets are read from the current project's conversation database.

