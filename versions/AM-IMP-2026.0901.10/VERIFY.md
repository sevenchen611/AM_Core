# Verify

1. Open an existing V5 with manager permission and verify every attachment has a visible red × without hover.
2. Confirm the × action still states that it creates the next version and retains old versions and Drive source files.
3. For a contract with three sent reviews and no archives, verify the page reports both counts and shows the prominent historical-backfill action.
4. Backfill confirmation must state that no LINE message will be resent.
5. After backfill, verify version-labelled archive links appear and each protected PDF opens.
6. Send a later draft and verify only the next non-overlapping conversation interval is captured automatically.
