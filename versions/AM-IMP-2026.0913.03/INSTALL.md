# Install

1. Apply the AMCore renderer, denied-route handler, and dry-run changes.
2. Apply the Rental finance-admin recovery query handling from its corresponding upgrade.
3. Run the checks in `VERIFY.md` in both clean repositories.
4. Merge reviewed pull requests to each repository's `main` branch before production deployment.

No database migration or new secret is required.
