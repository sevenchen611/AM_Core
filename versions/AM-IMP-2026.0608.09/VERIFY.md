# Verify

- `node --check src/server.js`
- Health JSON includes `immediateCommandEnabled: true`.
- Local or safe live LINE command can query tasks.
- After a task list, "打開第 2 個任務" can return detail.
- High-risk commands do not execute automatically.

