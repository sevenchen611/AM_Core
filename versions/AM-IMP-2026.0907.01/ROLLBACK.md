# Rollback

Revert this package and redeploy the preceding release. Existing group-binding
rows must not be deleted or moved. Rollback only restores the narrower command
normalization and removes the push fallback for future onboarding replies.
