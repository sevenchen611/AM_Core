# Rollback

Restore the previous Notion table view display settings inside the affected project only.

Do not delete `候選來源權責項目` if project users have started filling it in. Hide it from the view instead.

Keep the AMCore layout registry record unless the layout is intentionally deprecated.
