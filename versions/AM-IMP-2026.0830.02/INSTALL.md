# Install

1. Install the `pdf-lib` runtime dependency.
2. Deploy the updated draft-review service and public review page.
3. No database migration is required; existing review links obtain inline attachments from the stored immutable version snapshot.
