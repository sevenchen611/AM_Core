# Rollback

1. Restore the previous project-local `scripts/build-user-ui-connected-preview.js` from version control.
2. Regenerate the project User UI.
3. Keep archived Notion task records unchanged.

Rollback may cause archived tasks to reappear in generated User UI lists and task pages.

