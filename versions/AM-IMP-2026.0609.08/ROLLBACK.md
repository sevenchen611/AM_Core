# Rollback

Restore the previous project-local `scripts/build-user-ui-connected-preview.js` from version control and regenerate the User UI.

No Notion schema, task data, environment values, or production secrets are changed by this package.
