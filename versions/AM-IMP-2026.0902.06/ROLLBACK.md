# Rollback

Revert this package's code changes and redeploy the preceding release. Leave
`ENG_CONTRACTS_SIGNING_ENABLED=0` while rolling back. No schema or tenant data
rollback is required because this package performs no data migration.
