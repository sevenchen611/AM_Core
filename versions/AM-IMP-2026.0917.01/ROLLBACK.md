# Rollback

Revert this package through a reviewed main-branch commit and verify the actual target service. No records, migrations or files require deletion. Do not rewrite immutable approved/frozen/issued snapshots. Reverting reinstates the stale-master mismatch behavior, so avoid issuing affected contracts until the amount authority fix is restored.
