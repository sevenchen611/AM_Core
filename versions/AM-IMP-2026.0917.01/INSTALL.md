# Install

Apply the listed runtime files and regression tests to AM Platform. Run VERIFY.md, self-review the scoped diff, and deploy only a reviewed commit merged to GitHub main. No schema or environment changes are required. Existing saved version amounts take effect without editing live contract records. Do not copy tenant data into AMCore or install into standalone HOZO/Seven services for this Engineering-specific request.
