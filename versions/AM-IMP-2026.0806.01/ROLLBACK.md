# Rollback

1. Restore construction reminder links from `/task` to `/dashboard`.
2. Remove the `/task` construction route and task-card module.
3. Remove the task-card dry-run script and package command.

Existing handling records and images stay on their original Notion task pages and require
no data rollback. They remain visible from Notion and any future compatible task viewer.
